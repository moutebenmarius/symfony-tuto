{% extends 'admin/index.html.twig' %}

{% block title %}solution index{% endblock %}

{% block dynamic %}
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Gestion Solutions</h1>
          </div>
         
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            
            <!-- /.card -->

            <div class="card">
              <div class="card-header">
                <h3 class="card-title">Liste des Solutions</h3>
              </div>
              <!-- /.card-header -->
              <div class="card-body">
              {% for message in app.flashes('info') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
              <a href="{{ path('app_solution_new') }}" class="btn btn-sm btn-primary">Ajouter Solution</a>
               <table id="example1" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Description</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
       {% for solution in solutions %}
            <tr>
                <td>{{ solution.titre }}</td>
                <td>{{ solution.description }}</td>
                <td>{{ solution.prix }}</td>
                
                 <td>
                    <a class="btn btn-primary" href="{{ path('app_solution_show', {'id': solution.id}) }}"><i class="fas fa-eye"></i></a>
                      <a class="btn btn-info" href="{{ path('app_solution_edit', {'id': solution.id}) }}"><i class="fas fa-user-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
           </table>
               
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
  
{% endblock %}
