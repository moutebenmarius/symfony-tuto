

{% extends 'proviseur/index.html.twig' %}

{% block dynamic %}
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Gestion des cahiers</h1>
          </div>
          
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <div class="card">

              <div class="card-header">
                <h3 class="card-title">Liste des parties matiere <span class="alert alert-secondary m-5">{{ cahier.libelle }}</span></h3>
              </div>
               
              <!-- /.card-header -->
              <div class="card-body">
            
                <a href="{{ path('ajouter_partie_matiere',{'id': cahier.id}) }}" class="btn btn-sm text-white" style="background-color: teal"> + Ajouter partie matiere</a>
  <table class="table">
        <thead>
            <tr>
                <th>Enseignant</th>
                <th>Matière</th>
              
            </tr>
        </thead>
        <tbody>
          {% for pm in partieMatieres %}
              <tr>
                <td>{{ pm.enseignant.prenom }} {{ pm.enseignant.nom }}</td>
                <td>{{ pm.matiere}}</td>
              </tr>
          {% endfor %}
        </tbody>
    </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->

           
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
{% endblock %}
