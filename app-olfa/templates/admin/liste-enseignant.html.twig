{% extends 'admin/index.html.twig' %}

{% block dynamic %}
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Liste des enseignants</h1>
          </div>
          
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <div class="card">

              <div class="card-header">
                <h3 class="card-title">Liste des enseignant</h3>
              </div>
               <div class="card-body">
                  <form action="/enseignant/chercher" method="get" class="form-inline">
                    <div class="form-group">
                      <input type="text"  class="form-control" placeholder="recherche ..." name="q">
                    </div>
                    <div class="form-group">
                      <button type="submit" class="btn btn-sm btn-primary ml-2"><i class="fa fa-search" aria-hidden="true"></i></button>
                    </div>
                  </form>
              </div>
              <!-- /.card-header -->
              <div class="card-body">
                <table id="example2" class="table table-bordered table-hover">
                  <thead>
                  <tr>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Numéro de téléphone</th>
                    <th>Actions</th>
                  </tr>
                  </thead>
                  <tbody>
                {% for enseignant in enseignants %}
                  <tr>
                    <td>{{ enseignant.nom }}</td>
                    <td>{{ enseignant.prenom }}</td>
                    <td>{{ enseignant.telephone }}</td>
                    <td>
                        <a href="{{ path('app_enseignant_show', {'id': enseignant.id}) }}" class="btn btn-info"><i class="fa fa-eye"></i></a>&nbsp;
                        <a href="{{ path('app_enseignant_edit', {'id':enseignant.id}) }}" class="btn btn-primary"><i class="fa fa-edit"></i></a>
                        <a href="{{ path('archiver_enseignant', {'id': enseignant.id}) }}" class="btn btn-danger"><i class="fa fa-archive"></i></a>&nbsp;
                    </td>
                    
                  </tr>
                  {% endfor %}
                  </tbody>
                  
                </table>
                {% if retour %}
                    <a class="btn btn-primary btn-sm mt-3" href="{{ path('liste_enseignants') }}">Retour</a>
                  {% endif %}
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->

           
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
{% endblock %}