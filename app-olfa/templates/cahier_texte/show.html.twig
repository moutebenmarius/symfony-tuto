{% extends 'base.html.twig' %}

{% block title %}CahierTexte{% endblock %}

{% block body %}
    <h1>CahierTexte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cahier_texte.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ cahier_texte.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cahier_texte_index') }}">back to list</a>

    <a href="{{ path('app_cahier_texte_edit', {'id': cahier_texte.id}) }}">edit</a>

    {{ include('cahier_texte/_delete_form.html.twig') }}
{% endblock %}
